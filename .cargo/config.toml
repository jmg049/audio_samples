[build]
rustc-wrapper = ""

# Lint levels live in the portable `[lints]` table in Cargo.toml (edition 2024),
# not here: rustflags only apply to local builds in this checkout and would also
# leak `clippy::` lint names to plain `rustc` as noise.
